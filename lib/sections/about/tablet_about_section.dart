import 'package:flutter/material.dart';

class TabletAboutSection extends StatelessWidget {
  const TabletAboutSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/cupertino.dart';

class TabletContact extends StatelessWidget {
  const TabletContact({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

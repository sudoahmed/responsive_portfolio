import 'package:flutter/cupertino.dart';

class TabletPortfolio extends StatelessWidget {
  const TabletPortfolio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

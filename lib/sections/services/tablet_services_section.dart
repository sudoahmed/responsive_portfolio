import 'package:flutter/material.dart';

class TabletServicesSection extends StatelessWidget {
  const TabletServicesSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
